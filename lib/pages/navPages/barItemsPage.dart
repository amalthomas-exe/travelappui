import 'package:flutter/material.dart';

class BarItemsPage extends StatelessWidget {
  const BarItemsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Bar Item Page")),
    );
  }
}